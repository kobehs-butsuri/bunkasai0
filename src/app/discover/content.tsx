"use client"

import {useSetPageTitle} from "@/hooks/page-title-context";

export default function Policy() {
    useSetPageTitle("Discover")
    return (
        <div className="max-w-7xl mx-auto">
        </div>
    )
}
